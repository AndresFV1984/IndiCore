services:
  web:
    build:
      context: .
      dockerfile: Dockerfile
    image: bayronindicore/indicore:v1.2.0
    ports:
      - "5173:5173"
    restart: unless-stopped
    # Uncomment the volume below for local development to serve built files from host
    # volumes:
    #   - ./dist:/usr/share/nginx/html:ro
