server {
    listen 5173;
    root /usr/share/nginx/html;
    index index.html;

    location = / {
        return 302 /indicore/;
    }

    location /indicore/ {
        rewrite ^/indicore/(.*)$ /$1 break;
        try_files $uri $uri/ /index.html;
    }
}
